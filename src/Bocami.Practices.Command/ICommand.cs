namespace Bocami.Practices.Command
{
    /// <summary>
    /// A command changes state of an application. All commands are void, and don't return anything.
    /// </summary>
    public interface ICommand 
    {
    }
}
